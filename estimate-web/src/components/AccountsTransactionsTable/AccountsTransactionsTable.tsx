import React from "react";
import { Accounts } from "../../types/types";

interface Props {
  accounts: Accounts;
}

const AccountsTransactionsTable = ({ accounts }: Props) => (
  <div>Table comes here</div>
);

export default AccountsTransactionsTable;
